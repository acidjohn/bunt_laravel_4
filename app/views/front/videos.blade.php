<?php var_dump($videos); 
/*
<div class="tv-playlist currplaylist" data-list="{{$playlist->ytid}}">
	<h2>{{$playlist->title}}</h2>
	<div class="video-grid">
		@foreach ($videos as $video)
		<div class="tv-video-container">
			<a class="tv-video" data-vid="{{$video->ytid}}" data-list="{{$playlist->ytid}}">
			<img src="https://i2.ytimg.com/vi/{{$video->ytid}}/mqdefault.jpg">
			<div class="desc-panel">
				<h3>{{Band::find($video->band_id)->name}} : {{$video->title}}</h3>
			</div>
			</a>
		</div>
		@endforeach
	</div>
</div>
*/
?>